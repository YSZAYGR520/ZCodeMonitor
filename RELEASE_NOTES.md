# ZCode Monitor v1.0

ZCode / GLM Coding Plan 用量监控悬浮窗首个开源版本。

## ✨ 功能

- **额度监控**：5 小时块 / 周额度进度条（蓝紫渐变，超阈值变琥珀/红）、重置倒计时、
  消耗速率与外推预测、上下文水位、MCP 月度用量、峰谷徽章（高峰全额/非高峰5折）
- **用量统计**：KPI 总览、按模型算力分布、24h 趋势折线、
  模型速度明细（首 token / 耗时 / 吞吐三档着色 + 缓存命中率）
- **请求日志悬浮窗**：8 列（时间/模型/状态/耗时/输入/输出/命中率/命中-未命中），
  50 条记录、表头固定、数据区滚动；独立窗口，可拖动、可关闭
- **悬浮窗体验**：无边框置顶、顶栏拖动、高度自适应 + 底栏拖动调高（双击复位）、
  面板整体不滚动（模型速度卡内容超出时卡内独立滚动）、最小化到任务栏（期间暂停刷新省资源）、
  深浅主题、时间窗一键切换（今日/7天/30天/全部）、额度预警红点、单实例、设置注册表持久化
- **命令行工具**：总览 / 按模型 / 按项目 / 费用估算 / JSON 输出

## 📦 使用

克隆或下载本仓库后双击 `ZCodeMonitor.exe` 即可（仓库已附带，Windows x64，
无需安装任何环境）；被安全软件（如 Win11 智能应用控制）拦截时，可用
`python zcode_monitor_tk.py` 源码方式运行（需 Python 3.8+，零第三方依赖）。

## ⚠️ 注意

- 需本机已安装并登录过 ZCode CLI（读取 `~/.zcode/cli/db/db.sqlite`，只读不修改）
- 数据 100% 本地处理，官方接口仅查询额度，不上传任何信息
- 详见 README「注意事项与已知限制」
